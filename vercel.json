{
  "buildCommand": "python3 manage.py migrate && python3 manage.py populate_books && python3 manage.py createsuperuser --no-input --username admin --email admin@example.com --pass 123",
  "installCommand": "python3 -m pip install -r requirements.txt",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/wsgi.py"
    }
  ]
}
